'use client'

import { useState } from 'react'
import { AppLayout } from '@/components/app-layout'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Switch } from '@/components/ui/switch'
import { Badge } from '@/components/ui/badge'
import { Bot, Check, Copy, Menu, Send, Settings2 } from 'lucide-react'

const starterButtons = ['Новые закупки', 'По моим фильтрам', 'Избранное', 'Аналитика', 'Настройки']

export default function BotConfigPage() {
  const [botName, setBotName] = useState('Мой закупочный помощник')
  const [buttons, setButtons] = useState(starterButtons)
  const [alerts, setAlerts] = useState(true)
  return <AppLayout><div className="min-h-[calc(100vh-3.5rem)] overflow-auto p-4 lg:p-6"><div className="mx-auto flex max-w-7xl flex-col gap-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><Badge variant="outline" className="mb-2 border-primary/30 text-primary">Шаблон активен</Badge><h1 className="text-2xl font-semibold">Telegram-бот для закупок</h1><p className="mt-1 text-sm text-muted-foreground">Базовая логика уже собрана. Добавляйте фильтры, показатели и команды без программирования.</p></div><Button><Send data-icon="inline-start" />Опубликовать бота</Button></div><div className="grid gap-4 lg:grid-cols-[1fr_0.8fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Settings2 className="text-primary" />Основные настройки</CardTitle></CardHeader><CardContent className="flex flex-col gap-5"><div className="flex flex-col gap-2"><Label htmlFor="bot-name">Название бота</Label><Input id="bot-name" value={botName} onChange={(e) => setBotName(e.target.value)} /></div><div className="flex items-center justify-between rounded-lg border border-border p-4"><div><p className="font-medium">Уведомления о новых закупках</p><p className="text-sm text-muted-foreground">Отправлять совпадения по активным фильтрам</p></div><Switch checked={alerts} onCheckedChange={setAlerts} /></div><div><div className="mb-3 flex items-center justify-between"><div><p className="font-medium">Главное меню</p><p className="text-sm text-muted-foreground">Готовые кнопки из библиотеки модулей</p></div><Badge variant="secondary">{buttons.length} кнопок</Badge></div><div className="flex flex-col gap-2">{buttons.map((button, index) => <div key={button} className="flex items-center gap-2 rounded-lg border border-border bg-secondary/30 p-3"><Menu className="text-muted-foreground" /><Input value={button} onChange={(e) => setButtons((items) => items.map((item, i) => i === index ? e.target.value : item))} /><Button variant="ghost" size="icon" onClick={() => setButtons((items) => items.filter((_, i) => i !== index))}>×</Button></div>)}</div><Button variant="outline" className="mt-3" onClick={() => setButtons((items) => [...items, 'Новая кнопка'])}>Добавить кнопку</Button></div></CardContent></Card><Card className="mx-auto w-full max-w-sm bg-[#17212b] text-white"><CardHeader><div className="flex items-center gap-3"><div className="flex size-10 items-center justify-center rounded-full bg-primary"><Bot /></div><div><CardTitle className="text-base text-white">{botName}</CardTitle><p className="text-xs text-white/60">онлайн · предварительный просмотр</p></div></div></CardHeader><CardContent className="flex flex-col gap-4"><div className="rounded-2xl rounded-tl-sm bg-[#243447] p-4 text-sm">Здравствуйте! Я помогу найти подходящие закупки и покажу аналитику по каждой процедуре.</div><div className="grid grid-cols-2 gap-2">{buttons.map((button) => <Button key={button} variant="secondary" className="h-auto min-h-10 whitespace-normal text-xs" onClick={() => undefined}>{button}<Check data-icon="inline-end" /></Button>)}</div><div className="flex items-center gap-2 rounded-lg bg-[#243447] p-2"><Input placeholder="Напишите команду" className="border-0 bg-transparent text-white placeholder:text-white/50" /><Button size="icon"><Send /></Button></div><div className="flex items-center justify-between border-t border-white/10 pt-3 text-xs text-white/50"><span>Шаблон синхронизирован</span><Button variant="ghost" size="sm" className="text-white/70"><Copy data-icon="inline-start" />Копировать</Button></div></CardContent></Card></div></div></div></AppLayout>
}
