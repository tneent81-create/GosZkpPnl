'use client'
import { useMemo, useState } from 'react'
import { AppLayout } from '@/components/app-layout'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Slider } from '@/components/ui/slider'
import { Badge } from '@/components/ui/badge'
import { Calculator, ShieldCheck, WalletCards } from 'lucide-react'
export default function CalculatorPage(){const[price,setPrice]=useState(2400000),[cost,setCost]=useState(1680000),[expenses,setExpenses]=useState(180000),[tax,setTax]=useState(6);const result=useMemo(()=>{const taxValue=price*tax/100;const profit=price-cost-expenses-taxValue;return{profit,margin:price?profit/price*100:0,taxValue}},[price,cost,expenses,tax]);const money=(v:number)=>new Intl.NumberFormat('ru-RU',{style:'currency',currency:'RUB',maximumFractionDigits:0}).format(v);return <AppLayout><div className="min-h-[calc(100vh-3.5rem)] overflow-auto p-4 lg:p-6"><div className="mx-auto flex max-w-6xl flex-col gap-6"><div><Badge variant="outline" className="mb-2 border-primary/30 text-primary">Решение до подачи заявки</Badge><h1 className="text-2xl font-semibold">Калькулятор рентабельности</h1><p className="mt-1 text-sm text-muted-foreground">Маржа с учетом себестоимости, логистики, налогов и обеспечения.</p></div><div className="grid gap-4 lg:grid-cols-[1fr_0.8fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Calculator className="text-primary"/>Параметры закупки</CardTitle></CardHeader><CardContent className="flex flex-col gap-5"><div className="grid gap-4 sm:grid-cols-2"><div className="flex flex-col gap-2"><Label htmlFor="price">Цена контракта</Label><Input id="price" type="number" value={price} onChange={e=>setPrice(Number(e.target.value))}/></div><div className="flex flex-col gap-2"><Label htmlFor="cost">Себестоимость</Label><Input id="cost" type="number" value={cost} onChange={e=>setCost(Number(e.target.value))}/></div><div className="flex flex-col gap-2"><Label htmlFor="expenses">Логистика и накладные</Label><Input id="expenses" type="number" value={expenses} onChange={e=>setExpenses(Number(e.target.value))}/></div><div className="flex flex-col gap-2"><Label>Налоговая ставка: {tax}%</Label><Slider value={[tax]} onValueChange={v=>setTax(v[0]??6)} min={0} max={20} step={1}/></div></div><div className="rounded-lg border border-border bg-secondary/30 p-4 text-sm"><div className="flex items-center gap-2 font-medium"><ShieldCheck className="text-primary"/>Проверка безопасности</div><p className="mt-2 text-muted-foreground">Учтите обеспечение заявки, банковскую гарантию и кассовый разрыв.</p></div></CardContent></Card><Card className="bg-primary text-primary-foreground"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><WalletCards/>Итоговая модель</CardTitle></CardHeader><CardContent className="flex flex-col gap-5"><div><p className="text-sm opacity-80">Расчетная прибыль</p><p className="mt-1 text-4xl font-semibold">{money(result.profit)}</p></div><div className="grid grid-cols-2 gap-3"><div className="rounded-lg bg-primary-foreground/10 p-3"><p className="text-xs opacity-70">Маржа</p><p className="mt-1 text-xl font-semibold">{result.margin.toFixed(1)}%</p></div><div className="rounded-lg bg-primary-foreground/10 p-3"><p className="text-xs opacity-70">Налог</p><p className="mt-1 text-xl font-semibold">{money(result.taxValue)}</p></div></div><div className="border-t border-primary-foreground/20 pt-4 text-sm opacity-80">Модель обновляется в реальном времени.</div></CardContent></Card></div></div></div></AppLayout>}
